//styles
import styles from "./ResumeTitle.module.scss";

const HomeTitle = () => {
  return (
    <div className={styles.titleContainer}>
      <p className={styles.title}></p>
    </div>
  );
};

export default HomeTitle;
